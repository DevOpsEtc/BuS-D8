# env vars with only a key are resolved to their values on the machine Compose is running on, which can be helpful for secret or host-specific values
# get ip via $docker-machine ip [machineName]
MYSQL_DATABASE_HOST=
MYSQL_ROOT_PASSWORD=
MYSQL_DATABASE=
MYSQL_USER=
MYSQL_PASSWORD=
